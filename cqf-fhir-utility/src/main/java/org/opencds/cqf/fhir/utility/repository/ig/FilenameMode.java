package org.opencds.cqf.fhir.utility.repository.ig;

/**
 * This enum represents whether or not individual files in an IG are prefixed with the resource type.
 */
enum FilenameMode {
    ID_ONLY,
    TYPE_AND_ID,
}
